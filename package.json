{
  "name": "personal-static-website",
  "version": "1.0.0",
  "description": "Personal static website with portfolio and for-sale sections",
  "scripts": {
    "build": "npm run build:portfolio && npm run build:for-sale && npm run copy:root",
    "build:portfolio": "cd portfolio && npm install && npm run build",
    "build:for-sale": "cd for-sale && npm install && npm run build",
    "copy:root": "node scripts/copy-root.js",
    "dev:portfolio": "cd portfolio && npm run dev",
    "dev:for-sale": "cd for-sale && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "exifr": "^7.1.3",
    "fs-extra": "^11.1.1",
    "sharp": "^0.34.4"
  }
}
